{
  "name": "confusion",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "some command to be run",
    "test": "(eslint lib || true) && jest"
  },
  "dependencies": {
    "react": "^17.0.1",
    "react-dom": "^17.0.1",
    "react-scripts": "^4.0.1"
  }
}
